# FunASR CPU sidecar (ADR 0004). Install into a venv — do not mix into Node.
# torch CPU wheels are large; use the official CPU index if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
funasr>=1.1.12
modelscope>=1.11.0
torch
torchaudio
numpy
soundfile
librosa>=0.10.0
# Bundled ffmpeg binary (m4a/mp3/webm on Windows without system install)
imageio-ffmpeg>=0.4.9
